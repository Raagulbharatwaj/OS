#include<stdio.h>
#include<stdlib.h>

int* sort(int*,int);
void first_fit(int*);
void best_fit(int*);
void worst_fit(int*);

int main()
{

}

int* sort(int* array,int rev)
{
    if(rev==1):
    {
        
    }
}